# Phase 4 training stack for the RTX PRO 6000 box.
#
# Install order matters: install unsloth FIRST (its installer pins the
# transformers/torch pairing it patches), then this file:
#   pip install --upgrade unsloth
#   pip install -r requirements.txt
#
# unsloth is deliberately NOT listed here so pip cannot "resolve" it into a
# conflicting combination.

transformers>=4.46
datasets>=3.0
peft>=0.14
trl>=1.0            # v1.0+ unified post-training API incl. ORPOTrainer
accelerate>=1.0
bitsandbytes>=0.44  # adamw_8bit + NF4 for the QLoRA fallback / ORPO base
sentencepiece
protobuf
pyyaml>=6.0
pytest>=8.0         # test suite; cheap insurance on the training box too
