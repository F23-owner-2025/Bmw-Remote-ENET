# Legacy installers (not recommended)

These scripts required Rust or pre-built `.exe` files next to the script.

**Use `BMW-ENET-Setup.exe` from GitHub Releases instead.**

Kept only for developers debugging the old flow.
